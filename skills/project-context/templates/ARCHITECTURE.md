<!-- Cap: ~120 lines. Only what matters for reasoning about change impact. No secrets.
     Do not enumerate every function, endpoint, component, or DocType merely because it
     exists. Replace each hint comment with real content; delete sections that do not apply. -->

# Architecture

## Major modules

<!-- Each significant module and what it is responsible for. One line each. -->

## Key data

<!-- The DocTypes, models, or tables that carry the important business state, and what
     owns them. -->

## APIs and integrations

<!-- Endpoints other systems call, external services this project calls, and the
     boundary between them. -->

## Hooks, overrides, and jobs

<!-- Framework hooks, method or class overrides, background jobs, scheduled jobs. -->

## Critical flows

<!-- The few end-to-end paths where a regression would be expensive. -->

## Coupling and risky areas

<!-- Shared services and utilities many areas depend on, permission boundaries, state
     boundaries, migration-sensitive code, and anything already known to be fragile. -->
