{
  "description": "Enforces the push, staging, and live-site boundaries whether or not the orchestration skill loaded.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/guard.py",
            "args": [],
            "timeout": 10
          }
        ]
      }
    ]
  }
}
